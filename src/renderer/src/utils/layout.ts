/**
 * Re-export layout utilities for use in renderer.
 * Keeps shared/ as the single source of truth.
 */
export { autoColumns, computeLayout } from '@shared/layout'
export type { CellLayout } from '@shared/layout'
